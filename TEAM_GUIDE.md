# 팀원 시작 가이드

처음 해보는 사람 기준으로 썼습니다. 순서대로 따라 하면 됩니다.
막히면 혼자 30분 이상 붙잡지 말고 팀 채팅에 올리세요.

---

## 0. 이게 무슨 프로젝트인가

알바 계약서를 사진으로 찍으면,
- 뭐가 잘못됐는지 알려주고
- **사장님한테 뭐라고 말해야 하는지 문장까지 만들어주는** 앱

마지막 줄이 이 앱의 핵심입니다. 문제를 알려주는 앱은 많은데,
말을 못 꺼내서 그냥 넘어가는 게 진짜 문제라서요.

---

## 1. 준비물 (한 번만)

### 컴퓨터에 깔 것

**Node.js** — https://nodejs.org 에서 왼쪽 버튼(LTS) 눌러 설치.
설치 다 되면 확인합니다. 윈도우는 `cmd`, 맥은 `터미널`을 열고:

```
node -v
```

`v20.x.x` 같은 게 나오면 성공입니다. 아무것도 안 나오면 다시 설치하세요.

**VS Code** — https://code.visualstudio.com 에서 설치. 코드 쓰는 프로그램입니다.

### 폰에 깔 것

**Expo Go** — 앱스토어/플레이스토어에서 검색해서 설치.
우리가 만든 앱을 폰에서 바로 볼 수 있게 해주는 앱입니다.

---

## 2. 프로젝트 받기

전정현이 깃허브 주소를 알려줍니다. 터미널에서:

```
git clone https://github.com/ZAE0N/2026_starthon_main_project.git
cd 2026_starthon_main_project
git checkout develop
npm install
```

`git checkout develop` 을 빼먹지 마세요. 작업은 전부 `develop` 에서 합니다.

`npm install` 은 2~3분 걸립니다. 글자가 주르륵 올라가도 정상입니다.

그다음 환경 설정 파일을 만듭니다.

```
윈도우:  copy .env.example .env
맥:      cp .env.example .env
```

만든 `.env` 에서 **채울 건 토큰 한 줄뿐입니다.** 32자이고 전정현에게 받으세요.

```
EXPO_PUBLIC_API_TOKEN=<32자>
```

나머지는 그대로 두면 됩니다. 서버 주소는 코드에 기본값이 있어서 안 적어도 됩니다.

> 토큰을 비워두면 서버가 요청을 전부 막습니다. 앱에는 "지금은 분석할 수 없어요"
> 만 떠서 원인을 알 수 없습니다. 제일 많이 걸리는 곳입니다.

---

## 3. 실행해보기

```
npx expo start
```

터미널에 **QR코드**가 뜹니다.

- **안드로이드**: Expo Go 앱을 열고 "Scan QR code" 로 찍기
- **아이폰**: 기본 카메라로 QR 찍으면 Expo Go 가 열림

폰에 화면이 뜨면 성공입니다.

> **중요**: 컴퓨터와 폰이 **같은 와이파이**에 있어야 합니다.
> 학교 와이파이에서 안 되면 폰 핫스팟에 노트북을 연결해보세요.
>
> 컴퓨터에 와이파이가 없으면(유선만 쓰는 데스크톱) 이 방법이 안 됩니다.
> 그때는 `npx expo start --tunnel` 을 씁니다. `RUN_EXPO_GO.md` 에 정리해 뒀습니다.

### 이제부터는

코드를 고치고 **저장하면 폰 화면이 바로 바뀝니다.** 다시 실행할 필요 없습니다.

---

## 4. 내가 만들 파일 찾기

`app/` 폴더 안에 자기 담당 파일이 있습니다.

| 담당 | 파일 |
|---|---|
| 신우철 | `app/index.tsx`, `app/help.tsx` |
| 나영웅 | `app/camera.tsx`, `app/analyzing.tsx` |
| 정윤지 | `app/clause/[id].tsx`, `app/script.tsx` |
| 전정현 | `app/result.tsx`, `app/history.tsx`, 나머지 전부 |

**자기 파일만 고칩니다.** 남의 파일을 고치면 나중에 합칠 때 충돌납니다.

파일 이름이 곧 화면 주소입니다. `app/help.tsx` 를 만들면 `/help` 가 됩니다.

---

## 5. AI로 코드 만들기

이 프로젝트는 AI에게 시켜서 만듭니다. 그런데 **그냥 시키면 엉뚱한 코드가 나옵니다.**
반드시 아래 순서를 지키세요.

### 순서

**1) `AGENTS.md` 파일 내용을 통째로 복사해서 AI에게 먼저 붙여넣습니다.**
(Claude Code를 쓴다면 이 단계는 알아서 됩니다)

**2) 그다음 이렇게 요청합니다.**

```
위 규칙을 지켜서 app/help.tsx 하나만 만들어줘.

이 화면은 "도움받기" 화면이야.
- 혼자 해결이 어려울 때 보는 화면
- 상담 창구 2개를 카드로 보여주고, 누르면 전화가 걸림
- 전화번호와 운영시간은 constants/copy.ts 의 copy.help 에서 가져와
- 색과 글자 크기는 constants/theme.ts 에서만 가져와
- 새 라이브러리는 설치하지 마
```

**3) 나온 코드를 파일에 붙여넣고 저장 → 폰에서 확인**

**4) 마음에 안 들면 "이 부분을 이렇게 바꿔줘" 로 다시 요청**

### AI가 자꾸 틀리는 것

이런 게 보이면 잘못된 겁니다. 다시 시키세요.

| 잘못된 것 | 올바른 것 |
|---|---|
| `<div>`, `<span>`, `<button>` | `<View>`, `<Text>`, `<Pressable>` |
| `fontSize: "16px"` | `fontSize: 16` |
| `color: "#12294D"` | `color: colors.navy` |
| 글자를 그냥 두기 | 반드시 `<Text>` 로 감싸기 |
| `npm install 뭐시기` | 설치 금지 |

---

## 6. 자주 나는 에러

| 에러 화면에 이런 말이 | 뜻과 해결 |
|---|---|
| `Text strings must be rendered within a <Text>` | 글자를 `<Text>` 로 안 감쌌습니다 |
| `Unable to resolve module` | 파일 경로가 틀렸거나 패키지가 없습니다. 전정현에게 |
| `Cannot connect to Metro` | 폰과 컴퓨터가 다른 와이파이입니다 |
| 화면이 빨갛게 됨 | 맨 위 한 줄을 읽고, 모르겠으면 그대로 캡처해서 채팅에 |
| 아무 변화가 없음 | 터미널에서 `Ctrl+C` 후 `npx expo start -c` |

**에러가 나면 겁먹지 말고 캡처해서 올리세요.** 대부분 1분이면 해결됩니다.

---

## 7. 작업한 거 올리기 (매일 저녁)

하루 한 번, **미완성이어도** 올립니다. 몰아서 올리면 나중에 하루가 날아갑니다.

```
git checkout -b feat/help        (처음 한 번만. help 대신 자기 화면 이름)
git add .
git commit -m "도움받기 화면 만듦"
git push origin feat/help
```

그다음 깃허브 사이트에 들어가면 노란 줄에 **"Compare & pull request"** 버튼이
떠 있습니다. 눌러서 만들면 끝입니다. 합치는 건 전정현이 합니다.

> **PR 을 만들 때 받는 쪽(base)이 `develop` 인지 꼭 확인하세요.**
> 기본값이 `main` 으로 잡혀 있으면 바꿔야 합니다.

다음 날부터는 브랜치를 새로 만들 필요 없이 `add` → `commit` → `push` 만 하면 됩니다.

### 주의

- **`.env` 파일은 절대 올리지 마세요.** (자동으로 막아뒀지만 혹시 몰라서)
- **계약서 사진도 올리지 마세요.**

---

## 8. 서버 없이도 개발됩니다

**서버는 이미 배포돼 있어서 기본값은 진짜 판정입니다.** 계약서를 넣으면
6~7초 뒤에 실제 결과가 나옵니다.

서버를 안 타고 화면만 만들고 싶으면 `.env` 를 이렇게 바꿉니다.

```
EXPO_PUBLIC_USE_MOCK=true
```

그러면 **가짜 결과**가 2초 만에 나옵니다. 인터넷이 없어도 화면 작업이 됩니다.

> 다 만들었으면 반드시 `false` 로 돌려놓으세요. `true` 로 두면 어떤 계약서를
> 넣어도 결과가 똑같이 나옵니다. 판정이 안 되는 것처럼 보이는 가장 흔한 원인입니다.
> **분석이 2초에 끝나면 목 모드입니다.** 진짜는 6~7초 걸립니다.

### 에러 화면 만들 때 (나영웅)

에러 화면은 눈으로 봐야 만들 수 있는데, 가짜 결과는 항상 성공만 나옵니다.
그래서 일부러 에러를 내는 스위치를 넣어뒀습니다. `.env` 를 이렇게 고치고

```
EXPO_PUBLIC_MOCK_ERROR=timeout
```

터미널에서 `Ctrl+C` 후 **`npx expo start -c`** 로 다시 켜면 그 에러 화면이 뜹니다.
`timeout` 자리에 `network` `server` `unreadable` `notContract` 를 넣어 다섯 가지를 모두 확인하세요.
다 만들었으면 값을 다시 비웁니다.

> `-c` 를 빼면 `.env` 변경이 반영되지 않습니다. 아주 흔한 실수입니다.

---

## 9. 비개발자 (김종현)

`server/laws.json` 을 채우는 게 담당입니다. 코드가 아니라 **조사**입니다.

`TODO` 가 남은 항목은 **5개** 입니다.

| 항목 | 관련 법 |
|---|---|
| `hours` 근로시간 | 근로기준법 제50조 |
| `break` 휴게시간 | 근로기준법 제54조 |
| `weeklyPay` 주휴수당 | 근로기준법 제55조 |
| `penalty` 위약금 조항 | 근로기준법 제20조 |
| `required` 명시 항목 | 근로기준법 제17조 |

각각 이 두 가지를 채웁니다.

- `lawText` — 법 조문 원문 (국가법령정보센터 law.go.kr 에서 검색)
- `rule` — **어떻게 판단하는지 한 문장**

`rule` 이 제일 중요합니다. 예를 들어 이런 식입니다.

```
"4시간 근무 시 30분, 8시간 근무 시 1시간 이상의 휴게시간이
 없으면 위법소지. 휴게시간 언급 자체가 없으면 확인필요."
```

조문만 있으면 AI가 알아서 해석해야 해서 틀립니다.
판단 기준을 사람이 적어주면 정확해집니다.

**그리고 테스트용 계약서 3장**을 만듭니다.
일부러 잘못된 조항을 섞은 가짜 계약서입니다. 예를 들어

1. 시급을 9,800원으로 적은 것 (최저임금 미달)
2. 중도 퇴사 시 위약금 50만원 조항이 있는 것
3. 프리랜서 계약서인데 출퇴근 시간이 정해져 있는 것

---

## 10. 지금 바로 할 일

- Node.js, VS Code, Expo Go 설치
- 프로젝트 받아서(`git checkout develop` 잊지 말기) `npx expo start` 로 폰에 화면 띄우기
- 랜딩 → 촬영 → 분석 → 결과 → 조항 상세 → 말할 문장 까지 **직접 눌러보기**
- 자기 담당 파일 열어서 맨 위 주석의 "할 일" 확인
- (김종현) `server/laws.json` TODO 5개 채우기 + **테스트용 계약서 3종 제작**

화면 9개는 이미 동작하는 상태로 올라가 있습니다. 처음부터 만드는 게 아니라
**시안에 맞게 다듬는 작업**입니다.

**폰에 화면이 뜨고 끝까지 눌러지는 것까지가 오늘 목표입니다.**
안 되면 혼자 붙잡지 말고 바로 채팅에 올리세요.
