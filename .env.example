# 이 파일을 복사해서 .env 로 만들어 쓰세요.
#   cp .env.example .env
# .env 는 깃허브에 올라가지 않습니다.
#
# 값을 바꾼 뒤에는 반드시 캐시를 지우고 다시 시작해야 반영됩니다.
#   npx expo start -c

# 서버 주소. 끝에 / 를 붙이지 마세요. (붙이면 //inspect 가 됩니다)
# 배포된 서버는 기존 도메인에 경로를 얹은 형태입니다. 경로까지 포함해서 적습니다.
EXPO_PUBLIC_API_URL=https://smpsws.shop/albacheck

# 서버 /etc/albacheck.env 의 APP_TOKEN 과 같은 값을 넣습니다.
# ⚠ 한쪽만 채우면 모든 요청이 401 이 됩니다.
#   앱은 이 값이 비어 있으면 X-App-Token 헤더를 아예 보내지 않습니다.
#   그러면 앱에는 "지금은 분석할 수 없어요" 로만 보여 원인을 알 수 없습니다.
#   값은 전정현에게 받으세요. 저장소에 적지 않습니다.
EXPO_PUBLIC_API_TOKEN=

# ====================================================================
# 서버 없이 가짜 데이터로 개발할 때 true.
# 서버를 연결한 뒤에는 반드시 false 로 바꾸세요.
# 이걸 안 바꾸면 계속 같은 결과만 나옵니다. (매우 흔한 실수)
# ====================================================================
EXPO_PUBLIC_USE_MOCK=true

# 목 모드에서 일부러 에러를 내고 싶을 때 씁니다. (에러 화면 만들 때 필요)
# 비워두면 항상 성공합니다.
#
#   timeout     분석이 오래 걸리고 있어요
#   network     인터넷 연결을 확인해 주세요
#   server      지금은 분석할 수 없어요
#   unreadable  계약서를 읽지 못했어요
#   notContract 근로계약서가 아닌 것 같아요
#
# 값을 바꾼 뒤에는 반드시 npx expo start -c 로 다시 시작해야 반영됩니다.
EXPO_PUBLIC_MOCK_ERROR=
