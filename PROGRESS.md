# 진행 상황

> 알바 근로계약서 검진 앱 — 작업 로그
> 최종 갱신: 2026-09-12 / 작성·관리: 전정현

로드맵은 `README.md`, 개발 규칙은 `AGENTS.md`, 세팅 내역은 `SETUP.md` 를 봅니다.
이 파일은 **무엇이 끝났고 무엇이 남았는지**만 추적합니다.

---

## 현재 상태 한 줄

**팀원 4명이 지금 바로 작업을 시작할 수 있습니다.**
Expo 프로젝트가 서고, 화면 9개가 동작하는 상태로 만들어졌으며,
착수 전 감사에서 찾은 블로커와 코드 결함을 모두 고쳤습니다.
남은 가장 큰 일은 **서버(FastAPI + AI 호출)** 입니다.

---

## 완료

### 감사
- [x] 저장소 전체 감사 — 블로커 4건 / 중대 결함 9건 / 발표 리스크 7건
- [x] 감사 중 추가 발견 2건 (아래 "뒤늦게 찾은 것" 참고)

### 환경·구조
- [x] Node.js v24.19.0 + npm 11.17.0 설치 (감사 시점에 이 컴퓨터에 Node가 아예 없었음)
- [x] Expo SDK 57 프로젝트 생성 — **저장소 루트 = 프로젝트**
- [x] 템플릿의 `src/app/` → 루트 `app/` 으로 변경 (문서 전체가 `app/` 기준)
- [x] 템플릿의 `AGENTS.md`·`CLAUDE.md`·`constants/theme.ts` 유입 차단 (우리 파일과 이름 충돌)
- [x] `base_files/` 해체 → `types/` `constants/` `lib/` `server/` 로 `git mv`
- [x] 패키지 설치 (기존 4종 + `expo-image-manipulator`)
- [x] `.gitignore` 루트로 이동 + Expo 항목 보강 + 계약서 사진 보관 위치 정정
- [x] `.env` 생성

### 코드 결함
- [x] **C1** `lib/session.ts` 사진 슬롯 — `setCurrentPhoto` / `getCurrentPhoto` / `useCurrentPhoto`
- [x] **M3** `lib/api.ts` 타임아웃 25초 → **45초**
- [x] **M4** `lib/photo.ts` 긴 변 **1600px** 리사이즈 (+ 실패 시 원본 폴백)
- [x] **M5** `allUnknown` 오탐 수정 — 성공한 분석을 "읽지 못했어요"로 처리하던 문제
- [x] **M6** `EXPO_PUBLIC_MOCK_ERROR` 주입 — 에러 화면 6종을 서버 없이 재현 가능
- [x] 중복 호출 가드 (버튼 연타 = API 비용)
- [x] **M9** 목 결과 id 고정 → 매번 새 id
- [x] **R1** `constants/copy.ts` 개인정보 문구를 사실에 맞게 (외부 AI 전송 명시)
- [x] `types/index.ts` 에 `Photo` 타입 추가 (기존 필드는 그대로)
- [x] `lib/storage.ts` — SDK 57 에서도 `expo-file-system/legacy` 동작 확인, **수정 불필요**

### 화면
- [x] 화면 9개를 **동작하는 스텁**으로 생성 (빈 파일 아님)
      랜딩 → 촬영 → 분석 → 결과 → 조항 상세 → 말할 문장 → 기록함 → 도움받기 전 구간 연결
- [x] 각 파일 맨 위에 담당자와 "할 일" 주석

### 문서·데이터
- [x] **`README.md` §4 치명적 오류 수정** — 아래 "뒤늦게 찾은 것" 참고
- [x] **R3** 브랜치 접두사 `feat/` 로 통일
- [x] **R4** PR 타겟을 `develop` 으로 명문화 (세 문서 전부)
- [x] **M2** `laws.json` TODO **전부 해소** — 8개 항목 모두 `lawText` + `rule` 완비
- [x] **M1** 수습 감액에 단순노무 직종 제외 단서 반영 (+ `probationExcluded` 데이터 추가)
- [x] `TEAM_GUIDE.md` clone 절차·TODO 개수·PR 타겟·에러 화면 만드는 법
- [x] `SETUP.md` 현행화 (구조·패키지·세팅 근거)
- [x] `server/README.md` 45초·`law` 필수·M1 경고
- [x] **R5** 발표자료 "7개 항목" → 8개
- [x] `화면 시안.html` 상단에 보정 노트 6건 삽입

### 검증
- [x] `npx tsc --noEmit` — **에러 0건**
- [x] `npx expo export --platform android` — **번들 성공** (2.8MB, exit 0)
      실제로 실행되는 상태라는 뜻입니다

---

## 뒤늦게 찾은 것 (1차 감사에서 놓쳤던 것)

**1. `README.md` §4 가 팀원에게 틀린 스택을 알려주고 있었음** 🔴
AI에게 *"이 프로젝트는 **Next.js(App Router) + TypeScript + Tailwind** 를 사용한다"* 라고
입력하라고 안내하고 있었습니다. 실제로는 Expo React Native 입니다.
그대로 따랐으면 `<div>`·`className`·`"16px"` 같은 웹 코드가 나와서
**앱이 아예 실행되지 않습니다.** §2 의 "Tailwind 사용 가능", `styles/theme` 경로도 함께 틀렸습니다.
→ Expo 기준 프롬프트로 교체하고 경고 문구를 달았습니다.

**2. `.gitignore` 가 계약서 사진을 `assets/` 에 두라고 안내하고 있었음** 🔴
`assets/` 는 앱 아이콘이 들어가는 **커밋 대상** 폴더입니다.
김종현이 테스트 계약서를 거기 넣었으면 깃허브에 올라갔습니다.
→ `samples/` · `private/` 로 정정.

---

## 남은 일

### 서버 (최대 리스크, 전정현 담당)
- [ ] FastAPI `/inspect` `/health`, `X-App-Token`, 상태코드 5종
- [ ] `temperature: 0`, 프롬프트 인젝션 방어, `laws.json` 주입, 사진 미저장
- [ ] **`law` 필드를 반드시 채우게 할 것** — 비우면 앱이 근거 없는 판정처럼 보임
- [ ] **수습 감액 단서를 프롬프트에 넣을 것** — 빠뜨리면 위법을 "문제없음"으로 판정
- [ ] nginx 사용 시 `client_max_body_size 10M` — 기본 1MB 라 **작은 테스트 사진은 통과하고
      진짜 계약서 사진만 413 으로 실패**한다 (원인 추적이 가장 어려운 함정)

### 발표 준비
- [ ] **R6.** "89%" 와 "34.8%" 통계의 출처 확보. 문제 정의의 **유일한 근거**라
      출처를 못 대면 발표의 토대가 흔들림 (기획 문서가 스스로 두 번 유보를 달아 둠)
- [ ] **R2.** "만 18세 이상 전제" 와 청소년 타깃의 충돌에 대한 답변 준비
      (상담 창구 1순위가 청소년근로권익센터인데 판정은 성인 기준)
- [ ] **R1.** "사진이 외부 AI 로 가나요?" 질문에 대한 답변 준비
- [ ] 시연 영상, 발표자료

### 화면 다듬기 (팀원)
- [ ] 각자 담당 화면을 시안 + 보정 노트에 맞게

---

## 팀원 대기 사항

| 사람 | 지금 할 일 | 막고 있는 것 |
|---|---|---|
| 나영웅 | `camera.tsx` `analyzing.tsx` 다듬기, 에러 화면 6종 | **없음** |
| 정윤지 | `clause/[id].tsx` `script.tsx` 다듬기 | **없음** |
| 신우철 | `index.tsx` `help.tsx` 다듬기 | **없음** |
| 김종현 | `laws.json` 검증(`_verify` 표시), **테스트 계약서 3종** | **없음** |

`laws.json` 은 초안을 채워뒀으니 김종현은 **처음부터 조사하지 말고 law.go.kr 로 검증만** 하면 됩니다.
각 항목의 `_verify` 필드에 확인할 조문이 적혀 있습니다.

**김종현의 테스트 계약서 3종이 없으면 시연 자체를 못 합니다.** 코딩과 무관하므로 가장 먼저 시작해야 합니다.

---

## 결정 사항 로그

| 날짜 | 결정 | 이유 |
|---|---|---|
| 9/12 | 저장소 루트 = Expo 프로젝트 | 팀원이 clone 한 번으로 끝 |
| 9/12 | 화면은 루트 `app/` (SDK 57 기본은 `src/app/`) | 문서 전체가 `app/` 기준. 문서 수정 0건 |
| 9/12 | `expo-image-manipulator` 추가 | 해상도 축소 수단이 없었음. Expo 공식 패키지 |
| 9/12 | `typedRoutes: false` | 켜두면 `router.push(\`/clause/${id}\`)` 에서 타입 에러 |
| 9/12 | 타임아웃 25 → 45초 | 시연 중 타임아웃이 가장 큰 사고 |
| 9/12 | 사진 긴 변 1600px | OCR 품질 유지하면서 전송량 1/4 |

---

## 환경 메모

- **Node v24.19.0** (`C:\Program Files\nodejs`) / npm 11.17.0
- Expo SDK 57 · React Native 0.86 · React 19
- 작업 브랜치: `develop` (PR 타겟도 `develop`)
- 설치 직후 실행 중이던 터미널에는 PATH 가 반영되지 않음 → 새 터미널 필요

---

## 미해결 인지 사항

- **R7.** `EXPO_PUBLIC_API_TOKEN` 은 앱 번들에 평문으로 박힙니다. 누구나 추출할 수 있어
  "크레딧 소진 방지" 효과가 제한적 — 서버 측 rate limit 병행 권장. 해커톤 범위에선 감수
