# 진행 상황

> **CheckUp** — 알바 근로계약서 검진 앱 · 작업 로그
> 최종 갱신: 2026-09-13 / 작성·관리: 전정현

로드맵은 `README.md`, 개발 규칙은 `AGENTS.md`, 세팅 내역은 `SETUP.md` 를 봅니다.
서버 실행은 `server/run_local.md`, 배포는 `server/deploy/DEPLOY.md` 입니다.
폰에 띄우는 방법은 `RUN_EXPO_GO.md`, 회의 지적사항은 `REVIEW_2026-09-14.md` 입니다.
이 파일은 **무엇이 끝났고 무엇이 남았는지**만 추적합니다.

---

## 현재 상태 한 줄

**앱과 서버가 붙어서 실제로 판정이 나옵니다.**
화면 9개 전부 구현, 서버는 Lightsail 에 배포돼 HTTPS 로 열려 있습니다.
실제 계약서 사진으로 **7초 안에 8개 항목 판정**이 돌아옵니다.

남은 것은 **발표 준비**(영상, 자료, R6 통계 출처)와 **`laws.json` 법령 검증**입니다.

---

## 서버 (배포 완료)

| | |
|---|---|
| 주소 | `https://smpsws.shop/albacheck` |
| 상태 확인 | `https://smpsws.shop/albacheck/health` |
| 모델 | `gpt-5.4` — 계약서 4종 기준 4/4 정확, 평균 7초 (최대 8초) |
| 서비스 | `albacheck.service` — 재부팅에도 자동 실행 |

절차와 되돌리는 방법은 `server/deploy/DEPLOY.md` 에 있습니다.

### 판정 정확도 (계약서 4종 실측)

| 시나리오 | 기대 | 결과 |
|---|---|---|
| 최저임금 미달 (시급 9,500원) | 시급 = 위법소지 | 잡음 |
| 근로시간 초과 (주 60시간) | 근로시간·휴게시간 = 위법소지 | 둘 다 잡음 |
| 야간 교대 (8시간에 휴게 30분) | 휴게시간 = 위법소지 | 잡음 |
| 정상 계약서 | 위법소지 0건 | 0건 (오탐 없음) |

**최저임금과 수습 감액은 모델 판정을 그대로 쓰지 않습니다.**
모델에게는 사실만 읽게 하고(`facts`), 판정은 `laws.json` 의 `rule` 을 코드로 옮겨
서버가 다시 계산합니다 (`inspector.recompute`). 직종을 모르면 감액을 인정하지 않습니다.
M1(수습 감액 단서 누락)이 이 앱에서 가장 위험한 오류였기 때문입니다.

---

## 완료

### 화면 10개 — 전부 구현

| 화면 | 담당 | 비고 |
|---|---|---|
| `_layout.tsx` | 전정현 | 헤더·제목 일괄 관리. 화면에서 헤더 따로 만들지 말 것 |
| `index.tsx` | 신우철 | 4.2초마다 바뀌는 안내 카드, `copy.privacyShort` |
| `camera.tsx` | 나영웅 | 조건 질문 2개, 앱 안 카메라, 실시간 가이드 색(초록/회색), 권한 거부 안내 |
| `analyzing.tsx` | 나영웅 | 단계 문구(0/3/6/9초), 정보 카드 2.2초마다, 진행 막대(이어진 한 줄), 에러 6종 분기 |
| `marked.tsx` | 전정현 | 형광펜 그은 계약서. 문제 대목 글자 위만 칠함. 표시할 게 없으면 열리지 않음 |
| `result.tsx` | 전정현 | 문제없음 항목 접기, 썸네일에 표시 개수(눌러서 크게) |
| `clause/[id].tsx` | 정윤지 | 판정 배지, 3섹션, 빈 값 숨김 |
| `script.tsx` | 정윤지 | 톤 전환(정중한/명확한 말투). 9/14 에 복사·`followUp` 질문 제거 |
| `history.tsx` | 전정현 | 사진 썸네일, 이름 바꾸기, 삭제 |
| `help.tsx` | 신우철 | `copy.help` 순회, 전화 걸기 |

### 서버

- [x] FastAPI `/inspect` `/health`, `X-App-Token`, 상태코드 매핑
- [x] `laws.json` 주입, 프롬프트 인젝션 방어, 사진 미저장
- [x] `law` 필드는 AI 가 아니라 서버가 `laws.json` 에서 채움
- [x] 최저임금·수습 감액 판정을 서버가 재계산 (M1 대응)
- [x] 사진 정규화 — JPEG 변환, EXIF 회전 보정, 1600px 축소, HEIC 지원
- [x] Lightsail 배포 + HTTPS + systemd
- [x] `client_max_body_size 12M` (기본 1MB 면 진짜 계약서 사진만 413 으로 막힘)
- [x] 조항이 사진의 어디에 있는지 찾기 (`Clause.mark`) — 판정과 **동시에** 나가는
      별도 호출이라 응답 시간은 그대로(실측 6~8초)
- [x] 모델이 준 자리를 사진의 실제 글자 줄에 맞춤 (`server/textlines.py`, 15~25ms)
- [x] `seed` + `temperature=0` 으로 같은 사진에 같은 답
- [x] 근로시간·휴게시간·주휴수당도 코드가 다시 계산 (`inspector.recompute_time`)

### 사진 속 자리를 어떻게 찾았나 (`mark`)

문제 대목에 형광펜을 그으려면 그 내용이 사진의 어디에 있는지 알아야 합니다.
OCR 없이 모델에게 물어서 해결했는데, 그냥 물으면 안 됩니다. 실측 기록입니다.

| 방법 | 시나리오1 임금 줄 오차 | 결과 |
|---|---|---|
| 판정 프롬프트에 위치 요구를 같이 넣기 | 0.08 | 임금 대신 **임금지급일**에 줄이 그어짐 |
| 위치만 묻는 별도 호출 | 0.06~0.08 | 여전히 한 칸 아래 |
| + `temperature=0` | 0.08 (고정) | 재현은 되지만 **틀린 값이 고정됨** |
| + 왼쪽에 **세로 눈금자** | **0.004** | 칸을 정확히 맞춤 (4장 모두) |
| + 위쪽에 **가로 눈금자** | 세로 0.004 · 가로도 맞음 | 글자 줄 하나만 잡음 |

정답은 `0.303~0.356`(눈으로 잰 임금 칸)입니다.

핵심은 **모델이 좌표를 재지 않고 짐작한다**는 것이었습니다. 원본 사진에서는
맞히던 값이 앱이 보내는 1600px 정리본에서는 고정적으로 한 칸 아래였습니다.
눈금과 숫자를 그려주면 짐작할 일이 없어집니다. (`inspector._with_ruler`)

### 띠에서 형광펜으로 (같은 날 다시 고침)

처음에는 세로 위치만 받아 **사진 가로 전체에 띠**를 그었습니다. 모델이 가로를
못 맞힐 거라고 봤기 때문입니다. 그런데 실제 화면이 조잡했습니다.

- 띠가 항목 이름 칸까지 덮어서 형광펜이 아니라 색 슬래브로 보였습니다
- 표의 칸(셀) 전체를 잡게 요구해서 관계없는 줄이 같이 칠해졌습니다
- 번호 동그라미(22px)가 글자 한 줄(폰 너비에서 7px)보다 커서 사진을 덮었습니다

그래서 **가로에도 눈금자**를 붙이고, 프롬프트를 "칸 전체" 에서 "그 내용이 적힌
글자 줄만" 으로 바꿨습니다. 이제 글자 위만 칠합니다.

- 색은 분홍·노랑 계열에 알파를 넣었습니다(`constants/theme.ts` 의 `markStyle`).
  판정 3색(진한 red/amber)은 글자 위에 올리면 계약서가 안 읽힙니다.
  실제 형광펜이 분홍·노랑인 것과 같은 이유입니다
- **세로 여유는 0 입니다.** 줄 간격이 좁아(글자 한 줄이 사진 높이의 0.02) 위아래로
  0.004 만 넓혀도 다음 줄이 같이 칠해졌습니다. 가로만 0.005 넉넉하게 긋습니다
- 번호 동그라미는 사진에서 뺐습니다. 아래 목록의 색칠로 잇습니다
- 썸네일(56px)에도 형광펜을 그리지 않습니다. 그 크기에서는 색 점으로만 보입니다.
  대신 표시 개수를 얹어서 눌러볼 이유를 줍니다

**사진을 고치는 게 아닙니다.** 사진은 그대로 두고 그 위에 반투명한 `View` 를
얹습니다(`components/MarkedShot.tsx`). 사진 픽셀은 건드리지 않습니다.

틀릴 것 같으면 아예 그리지 않습니다. 값이 범위를 벗어나거나 뒤집혀 있거나
높이가 페이지의 10% 를 넘으면 버립니다
(`inspector._to_mark`, `lib/api.ts` 의 `toMark`).

### 마지막 ±0.01 은 사진에서 직접 찾습니다 (`server/textlines.py`)

눈금자를 붙여도 ±0.01 이 남습니다. 계약서 줄 간격이 0.02 라서 그 정도만
어긋나도 옆 줄을 물거나 반쯤 걸칩니다. 실제로 주휴일 칸에 그은 형광펜이
연차유급휴가 줄까지 걸쳤습니다.

그래서 **모델 좌표를 힌트로만 쓰고, 사진에서 글자 줄을 직접 찾아 맞춥니다.**

1. 사진을 900px 로 줄이고 흑백으로 바꿉니다
2. Otsu 로 글자/배경 문턱값을 정합니다 — 고정값(128)을 쓰면 그늘진 사진에서
   종이 전체가 글자로 잡힙니다
3. 행마다 잉크 비율을 세서 글자 줄의 위·아래를 찾습니다
4. 모델이 준 자리와 가장 많이 겹치는 줄에 맞춥니다
5. 가로는 그 줄 안의 잉크로 **줄이기만** 합니다. 늘리면 표의 항목 이름 칸이나
   세로 칸선까지 물 수 있어서 안전한 방향으로만 움직입니다

픽셀을 파이썬 루프로 세지 않습니다. `resize(BOX)` 가 행·열 평균을 C 로
계산해 주므로 **15~25ms** 에 끝납니다.

못 찾거나 찾은 줄이 모델 값과 너무 다르면(0.35배~3배 밖) **손대지 않습니다.**
사진이 기울었거나 그늘이 심하면 줄 찾기가 어긋나는데, 그때 억지로 맞추면
지금보다 더 엉뚱한 자리에 칠하게 됩니다.

결과: 시나리오 4장 모두 높이가 0.012(글자 한 줄)로 줄고 줄 위에 정확히 얹힙니다.

**아직 확인하지 못한 것**: 위 숫자는 모두 **화면에서 렌더한 계약서**로 잰
것입니다. 실제 종이를 폰으로 찍으면 기울어지고 그림자가 지므로 더 나빠질 수
있습니다. 실물 촬영 검증이 남아 있습니다. 다만 줄 맞춤이 실패하면 모델 값을
그대로 쓰므로, 나빠져도 이 기능 도입 전보다 못해지지는 않습니다.

### 같은 사진에 같은 답 (2026-09-15)

"같은 계약서를 찍었는데 결과가 다르게 나온다" 는 지적을 받고 재봤습니다.
같은 파일을 3번씩 배포 서버로 보냈습니다.

| 사진 | 흔들린 항목 |
|---|---|
| 시나리오1 최저임금미달 | 0/8 |
| 시나리오2 근로시간초과 | 0/8 |
| 시나리오3 야간휴일수당 | **1/8** — 주휴수당 문제없음 / 문제없음 / 확인필요 |
| 시나리오4 정상계약서 | 0/8 |

즉 **판정 자체는 거의 결정적**이고, 사용자가 겪은 차이는 대부분 다시 찍을
때마다 사진이 달라지기 때문입니다. 그래도 남은 것은 코드로 없앴습니다.

- **`seed` 추가** — `temperature=0` 만으로는 부족합니다. 보장은 아니지만 공짜입니다
- **근로시간·휴게시간·주휴수당·명시항목을 코드가 다시 계산** (`recompute_time`).
  최저임금에 쓰던 방식(모델은 사실만, 판정은 코드)을 넷에 더 적용했습니다.
  새로 읽는 사실: `dailyWorkHours`, `dailyShiftHours`, `workDaysPerWeek`,
  `breakMinutes`, `weeklyPaidHoliday`, `statedItems`

고친 뒤 같은 방법으로 다시 재서 **4장 32개 전부 고정**을 확인했습니다.

고치는 중에 휴게시간이 새로 흔들리는 것을 발견했습니다. 시나리오3 은
`22:00~06:00`, 휴게 30분, "1일 소정근로시간 8시간" 인데 **계약서 자체가
앞뒤가 안 맞습니다.** 적힌 8시간을 쓰면 휴게 1시간이 필요해 위법이고,
시각에서 휴게를 빼 7.5시간으로 보면 30분으로 충분합니다. 모델이 그때그때
다른 쪽을 골랐습니다.

그래서 하루 근로시간을 **세 경로로 교차 확인**하고 가장 큰 값을 씁니다.

  적힌 값 · (시각 − 휴게) · (주 소정근로시간 ÷ 주 근무일수)

주 단위 값이 가장 안정적으로 읽혔고(3회 모두 40시간), 애매하면 근로자에게
유리한 쪽으로 봅니다.

⚠ **코드는 위로만 덮습니다.** `laws.json` 의 rule 에는 숫자로 못 잡는 조건이
많습니다(휴게시간을 근무 시작 전에 배치, 대기시간을 휴게로 적음, 시급에
주휴수당이 포함됐다고 적음). 모델은 그런 걸 잡는데 코드는 못 잡습니다.
그래서 코드가 "문제없음" 이라고 해서 모델의 위법 판정을 내리지 않습니다.
올릴 때만 덮습니다. 예외는 주휴수당의 "주 15시간 미만" 하나로, 법이 아예
적용되지 않는 경우라 내려도 됩니다.

### 놓치기 쉬운 조건 — 이제 실제로 동작합니다 (2026-09-15)

촬영 화면에서 두 가지를 묻고 판정에 반영합니다. `components/WorkplaceQuestions.tsx`

```
일하는 사람이 몇 명인가요?   [5명 미만] [5명 이상] [모르겠어요]
만 18세 미만인가요?          [예] [아니오]
```

**그전까지는 이 기능이 죽어 있었습니다.** `conditions.py` 와 `laws.json` 의
`conditions` 는 채워져 있었지만 질문 칩이 없어서 답이 항상 `null` 로 갔습니다.
즉 5명 미만 사업장 사용자에게 **없는 위법을 알려주고 있었습니다.**

답을 강제하지 않습니다. 안 골라도 촬영 버튼은 눌리고, 그때는 5인 이상·만 18세
이상 기준으로 봅니다. 미성년 기준이 더 엄격해서, 성인인데 그 기준으로 보면
없는 위법을 만들어냅니다. 위반을 놓치는 것보다 그게 더 나쁩니다.

### 법령 검증 — 근로기준법 시행령 별표1 원문 대조 (2026-09-15)

`conditions` 의 값은 제가(AI) 조사한 것이라 `_verify` 로 대조 요청을 달아뒀었습니다.
law.go.kr 의 **별표1 PDF 원문**을 열어 전부 확인했고 `_verify` 를 결과로 바꿨습니다.

별표1 "상시 4명 이하의 근로자를 사용하는 사업 또는 사업장에 적용하는 법 규정"

| 장 | 적용되는 조문 |
|---|---|
| 제1장 총칙 | 제1조~제13조 |
| 제2장 근로계약 | 제15조, **제17조**, **제18조**, 제19조제1항, **제20조~제22조**, 제23조제2항, 제26조, 제35조~제42조 |
| 제3장 임금 | 제43조~제45조, 제47조~제49조 |
| 제4장 근로시간과 휴식 | **제54조, 제55조제1항, 제63조** — 이것뿐 |
| 제5장 여성과 소년 | 제64조, 제65조제1항·제3항, **제66조~제69조**, **제70조제2항·제3항**, 제71조, 제72조, 제74조 |
| 제6장 안전과 보건 | 제76조 |
| 제8장 재해보상 | 제78조~제92조 |

**우리 8개 항목 중 5인 미만에서 빠지는 것은 `hours`(제50조) 하나뿐입니다.**

- 제4장 목록에 제50조·제53조·제56조·제60조가 **없다** → 주 40시간 한도, 연장
  한도, 가산수당, 연차가 5인 미만에 적용되지 않는다. `appliesTo: ["hours"]` 가 맞다
- 휴게(제54조)와 주휴일(제55조 제1항)은 **있다** → 규모와 무관하게 적용
- 위약 예정 금지(제20조)와 근로조건 명시(제17조)도 **있다** → `penalty`·`required`
  를 덮지 않는 것이 맞다
- **제69조(18세 미만 1일 7시간·주 35시간)가 있다** → 5인 미만이어도 미성년
  기준은 그대로다. 그래서 `_apply_minor_hours` 가 `appliesTo` 보다 나중에 돌아
  `hours` 를 다시 올린다
- 최저임금법은 별표1 의 대상이 아니라 규모와 무관

같은 사진(주 60시간·휴게 30분)에 답만 바꿔 실제로 돌려본 결과다.

| 답 | hours | break | weeklyPay |
|---|---|---|---|
| 모름 | 위법소지 | 위법소지 | 확인필요 |
| 5명 이상 · 성인 | 위법소지 | 위법소지 | 확인필요 |
| 5명 미만 · 성인 | **문제없음** | 위법소지 | 확인필요 |
| 5명 미만 · 만18세미만 | **위법소지** | 위법소지 | 확인필요 |

### 판정을 덮을 때 설명도 같이 바꿉니다

`conditions` 가 판정을 덮을 때 `verdict` 만 바꾸고 `plain` 은 그대로 뒀습니다.
그래서 5인 미만으로 `hours` 를 문제없음으로 내리면 배지는 "문제없음" 인데
설명은 "주 60시간이라 한도를 넘어요" 가 남았습니다. **모순입니다.**

`laws.json` 의 `appliesToText` 와 `Decision.override_texts` 를 추가해서 문장도
같이 바꿉니다. 문장이 없으면 서버가 기본 문장을 씁니다.

### 촬영 화면의 실시간 가이드 색 — 폰과 웹이 보는 것이 다릅니다

가이드 네모가 맞으면 초록, 아직이면 회색입니다. 그런데 **무엇을 보고 판단하는지가
플랫폼마다 다릅니다.** 같은 색에 다른 뜻이 붙으므로 문구도 다르게 씁니다.

| | 무엇을 보는가 | 초록의 뜻 |
|---|---|---|
| 웹 (`lib/frameFit.web.ts`) | 미리보기 화면을 실제로 읽음 | 계약서가 네모 안에 들어옴 |
| 폰 (`lib/frameFit.ts`) | 가속도 센서 | 폰이 좌우로 기울지 않음 |

**왜 폰에서는 화면을 못 읽는가**: Expo Go 에서 카메라 미리보기의 프레임을
받아올 방법이 없습니다. `expo-camera` 에 그 기능이 없고, 있는
`react-native-vision-camera` 는 개발 빌드(EAS)가 필요해서 QR 로 여는 방식
자체가 바뀝니다. `takePictureAsync` 를 반복하는 방법은 `fastMode`·
`maxDownsampling` 이 `@hidden` 내부 API 이고, 그걸 써도 JPEG 을 픽셀로 풀
방법이 없습니다.

**왜 기울기라도 보는가**: 비뚤게 찍힌 사진이 판독을 가장 많이 망칩니다.
글자가 기울면 모델이 표를 잘못 읽고 서버의 줄 찾기(`textlines.py`)도
어긋납니다. 좌우 기울기만 봅니다 — 앞뒤는 계약서가 책상에 있는지 벽에
있는지 알 수 없어서 판단할 수 없습니다.

**웹에서 무엇을 종이로 보는가**: 계약서는 밝은 종이입니다. Otsu 로 밝고 어두운
경계를 정하고 밝은 덩어리의 테두리를 찾습니다. 초록은 셋을 다 만족할 때만입니다.

1. 종이가 네모 안에 있다 (약간의 여유 허용)
2. 네모를 충분히 채운다 (55% 이상) — 멀리서 작게 찍으면 글자를 못 읽습니다
3. 화면 가장자리에 닿지 않는다 — 닿으면 잘렸다는 뜻입니다

덩어리의 가로÷세로가 0.45~1.6 밖이면 종이로 보지 않습니다. 조명 반사처럼
밝지만 종이가 아닌 것에 초록이 켜지는 것을 막습니다.

합성 이미지로 재봤습니다(오차는 1px 수준).

| 상황 | 결과 |
|---|---|
| 종이가 가운데 크게 | 찾음 (오차 0.008) |
| 종이가 왼쪽으로 치우침 | 찾음 → 네모 밖이라 **회색** |
| 종이가 화면 밖으로 잘림 | 찾음 → 가장자리에 닿아 **회색** |
| 배경도 흰색 (대비 없음) | 화면 전체가 잡힘 → 가장자리에 닿아 **회색** |
| 가로로 긴 밝은 띠 | 비율이 안 맞아 **무시** |
| 종이가 너무 작음 | 못 찾음 → **회색** |

**빨강을 쓰지 않았습니다.** 계약서를 아직 못 맞춘 것은 고장이 아니라 진행
중이고, 빨강은 뭔가 잘못됐다는 뜻으로 읽힙니다. 회색입니다.

색만으로 알려주지 않습니다. 네모 위 문구가 상태에 따라 같이 바뀝니다.

### 2026-09-15 결정 — 결과 화면의 면책 문구를 뺐습니다

"확정된 법적 판단이 아니에요. 최종 확인은 노무사나 고용노동부를 통해 하세요."

판정 근거를 화면에서 이미 다 보여주는데(조문 번호 + 조문 전문 + 판정 전제),
그 아래에 이 문구가 붙으면 근거가 없는 것처럼 읽힙니다. "데이터 없이 만든
프로젝트" 로 보일 여지를 주지 않기 위해 뺐습니다.

한계를 감추는 게 아닙니다. 심사에서 지적이 들어오면 개선점과 추후 방향성으로
답합니다. 화면에 상시로 붙이지 않을 뿐입니다. `constants/copy.ts` 에 경위를
주석으로 남겼습니다. **되살리지 마세요.**

### 구현 중 발견해 고친 것

| | 내용 |
|---|---|
| 앱의 숨은 규칙 | 8개가 전부 "확인필요" + 원문 전부 빈 값이면 앱이 200 을 버리고 에러를 띄움(`lib/api.ts:191-194`). 판독 실패 시 200 대신 **422** 를 반환하게 함 |
| FastAPI 기본 422 | 요청 검증 실패의 기본값이 422 인데, 앱에서 422 는 "다시 찍어주세요". 필드명 오타가 사용자에게 재촬영을 유도함 → **400** 으로 오버라이드 |
| OpenAI SDK `max_retries` | 기본값 2. 그대로 두면 30초 타임아웃이 90초가 되어 앱이 먼저 끊김 → 0 |
| `temperature` 거부 | 일부 모델이 `temperature` 를 아예 안 받음. 거부하면 빼고 재시도하고 그 모델을 기억 |
| 저장 실패로 결과 유실 | `AsyncStorage` 쓰기 실패가 "server" 에러로 잡혀 성공한 판정이 버려졌음 → 기록만 건너뛰고 결과는 반환 |
| 권한 거부 구분 | `takePhoto` 가 취소와 권한 거부를 모두 `null` 로 반환. `getPhotoPermission()` 추가 |
| 근로시간 오판 | 정확히 8시간·주 40시간을 위법으로 보는 사례 → `laws.json` `hours` rule 에 한도 내라는 문장 추가 |

### 서버 정리 (Lightsail)

배포 전 메모리 여유가 343MB 뿐이었습니다. 쓰이지 않는 서비스 3개를 중지했습니다.

| 중지한 것 | 확보 | 근거 |
|---|---|---|
| `java -jar demo-0.0.1-SNAPSHOT.jar` | 510MB | systemd 유닛 없이 손으로 띄워 114일 방치. 외부 접근 불가 |
| `guidance.service` (8000) | 90MB | 14일간 요청 0건. 외부 접근 불가 |
| `eatzy-backend.service` (8001) | 132MB | 14일 로그가 전부 봇 스캐너 + 404 |

여유 343MB → **1,009MB**. 코드와 데이터는 지우지 않았고 `disabled` 로 두었습니다.
**`exam-gen`(8002), `inhatc-api`(8010) 는 `smpsws.shop` 에서 실제로 쓰이므로 건드리지 않았습니다.**

---

## 남은 일

### 발표 준비 (가장 급함)

- [ ] **R6.** "89%" 통계의 출처 확보. 랜딩에 큰 숫자로 들어가 있고 문제 정의의 **유일한 근거**입니다.
      출처를 못 대면 발표 토대가 흔들립니다.
      나영웅은 같은 이유로 `analyzing.tsx` 에서 "34.8%" 를 빼고 법 조문으로 대체했습니다.
      **한쪽은 빼고 한쪽은 강화한 상태라 기준을 통일해야 합니다.**
- [ ] **R2.** "만 18세 이상 전제" 와 청소년 타깃의 충돌에 대한 답변
      (상담 1순위가 청소년근로권익센터인데 `assumptions` 는 성인 기준)
- [ ] **R1.** "사진이 외부 AI 로 가나요?" 답변 — 문구는 정직하게 고쳐둠
- [ ] 시연 영상 녹화, 발표자료

### 법령 검증 (김종현)

- [ ] `laws.json` 의 `_verify` 8건을 law.go.kr 에서 확인
      **처음부터 조사하지 말고 검증만** 하면 됩니다. 초안은 다 채워져 있습니다.

### 개선 방향 (발표에 넣을 것)

지금 범위에서 못 한 것들입니다. 구조적인 이유가 있습니다.

- **제56조 가산임금 항목 추가** — 야간·휴일·연장 가산임금을 판정하지 못합니다.
  항목이 8개에서 9개가 되면 `types/index.ts`·서버 스키마·화면 문구가 같이 바뀌어
  발표 직전에는 위험합니다. 실제로 시나리오 3의 핵심 위법이 이것입니다.
  **9/14 결정: 판정하지 않고 안내로만 다룹니다.** "놓치기 쉬운 조건 안내" 기능의
  "몰랐을 수도 있는 것" 에 넣습니다 (`FEATURE_hidden-conditions-design.md`).
  발표에서 야간수당을 판정한다고 말하지 않습니다. 못 잡습니다.
- **판정 속도** — 현재 7초. 8개 항목을 2~3개 호출로 쪼개 병렬화하면 3초 이내가 가능합니다.
  비용이 늘고 항목 간 일관성 처리가 필요해 이번 범위에서 제외했습니다.
- ~~**커스텀 카메라** (`expo-camera`)~~ — **9/15 완료.** 앱 안에서 미리보기를
  띄우고 초록 가이드 네모(A4 비율)를 겹칩니다. 실패하면 폰 기본 카메라로
  되돌아갑니다.
- **행동 지표 수집** — 사용자가 실제로 사장님께 말을 꺼냈는지 셀 수 없습니다.
  9/14 까지 `script.tsx` 에 "사장님께 말해보셨나요?" 질문이 있었지만, 답이 폰 안
  `AsyncStorage` 에만 남아 **우리는 볼 수 없었습니다.** 앱이 서버로 보내는 요청은
  `/inspect` 와 `/health` 두 개뿐이고 분석 도구도 붙어 있지 않습니다.
  집계할 수단이 없는 지표였으므로 질문 자체를 뺐습니다.

  제대로 하려면 `POST /event` 하나와 저장소가 필요합니다. SQLite 파일이면 충분합니다
  (`(익명ID, 조항, 답, 시각)` 네 칸. Lightsail 디스크 여유 52GB). 반나절 일입니다.

  **이번에 안 한 이유는 기술이 아니라 표본입니다.** 제출까지 실사용자를 모을 수 없어
  숫자가 0에 가깝게 나옵니다. 그리고 사용자 행동을 서버로 보내기 시작하면
  "사진이 외부 AI 로 가나요"(R1) 답변에 설명할 항목이 늘어납니다.
  지금은 "사진을 저장하지 않는다" 로 깔끔합니다.

  **발표에서 이 지표를 측정했다고 말하면 안 됩니다.** 측정한 적이 없습니다.
- **미성년 기준 판정** — `assumptions` 가 만 18세 이상 고정입니다. 나이 입력 UI 가 필요합니다.
  **9/14 착수.** "놓치기 쉬운 조건 안내" 가 이걸 해결합니다 (R2 도 같이 없어집니다).
  서버·타입·결과 화면은 붙었고, 질문 UI(나영웅)와 `laws.json` 의 `conditions`(김종현)가 남았습니다.

---

## 결정 사항 로그

| 날짜 | 결정 | 이유 |
|---|---|---|
| 9/12 | 저장소 루트 = Expo 프로젝트 | 팀원이 clone 한 번으로 끝 |
| 9/12 | 화면은 루트 `app/` | 문서 전체가 `app/` 기준 |
| 9/12 | `expo-image-manipulator` 추가 | 해상도 축소 수단이 없었음 |
| 9/12 | `typedRoutes: false` | 켜두면 `router.push(\`/clause/${id}\`)` 에서 타입 에러 |
| 9/12 | 타임아웃 25 → 45초 | 시연 중 타임아웃이 가장 큰 사고 |
| 9/12 | 사진 긴 변 1600px | OCR 품질 유지하면서 전송량 1/4 |
| 9/13 | 모델 `gpt-5.4` | 4종 실측 4/4·7초. `gpt-5.5` 도 4/4 지만 19초(최대 26초) |
| 9/13 | 시급·수습은 서버가 재계산 | 프롬프트만으로는 M1 오판을 막을 수 없음 |
| 9/13 | 기존 도메인에 `/albacheck/` 경로 추가 | 인증서 재발급·방화벽 개방 불필요 |
| 9/13 | 우리 uvicorn 은 `127.0.0.1:8003` | nginx 우회로 HTTPS·크기제한을 건너뛰는 통로를 만들지 않음 |
| 9/13 | 발표는 영상으로 | 현장 네트워크에 의존하지 않음 |
| 9/14 | 야간수당은 안내 전용 | 제56조를 9번째 항목으로 올리면 계약서 4종을 다시 다 재야 함. 제출까지 시간이 없음 |

---

## 환경 메모

- **Node v24.19.0** / npm 11.17.0 · Expo SDK 57 · React Native 0.86 · React 19
- 서버: Ubuntu 24.04 · Python 3.12.3 · `/opt/albacheck/`
- 작업 브랜치: `develop` (PR 타겟도 `develop`). 브랜치 접두사는 `feat/`
- `.env` 값을 바꾼 뒤에는 **반드시 `npx expo start -c`** (캐시 때문에 반영 안 됨)

---

## 미해결 인지 사항

- **R7.** `EXPO_PUBLIC_API_TOKEN` 은 앱 번들에 평문으로 박힙니다. 누구나 추출할 수 있어
  "크레딧 소진 방지" 효과가 제한적입니다. **진짜 방어선은 OpenAI 대시보드의 지출 한도**입니다.
  서버 측 rate limit 병행을 권하지만 해커톤 범위에선 감수합니다.
- **문서 불일치 (미수정)** — 급하지 않아 남겨둡니다.
  - `TEAM_GUIDE.md` 가 "`laws.json` TODO 5개 채우기" 로 안내 — 이미 다 채워져 있어 중복 작업 유발
  - `AGENTS.md:250` 에 PR 타겟 `develop` 언급 없음 (다른 세 문서에는 있음)
  - `SETUP.md` 는 패키지 5개로 안내, 실제 25개
  - `package.json` 의 `reset-project` 스크립트가 없는 파일을 가리킴
