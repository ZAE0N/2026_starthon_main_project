# 회의 지적사항 — 2026-09-14

> 회의에서 나온 것을 그대로 옮긴 기록입니다. **고치는 작업은 나중에 합니다.**
> 지금은 잊지 않게 남겨두는 것이 목적입니다.
>
> 전부 전정현이 처리하기로 했으므로 담당자로 나누지 않고 화면별로 묶었습니다.
> 진행 상황은 `PROGRESS.md`, 화면별 담당은 `SETUP.md` 의 구조표를 봅니다.

---

## UI

| 화면 | 지적 |
|---|---|
| 앱 로딩(스플래시) | 못생김 |
| 메인 | 89% 하나만 딱 있는 게 이상함. 카드를 추가하는 등 보강 필요.<br>문구가 너무 상단에 몰려 있음 — 아래로 내릴 필요 |
| 분석중 | 로딩 중 정보 카드가 넘어가지 않음 |
| 결과 | 왼쪽 상단 사진을 눌렀을 때 전체 보기로 크게 볼 수 있어야 함.<br>상단에 뜨는 가게 종류 표시가 이상하고 정보가 틀림 |
| 보관함 | 알림 팝업이 너무 딱딱함 |
| 카메라 | 가이드라인이 있어야 함 |
| 문장 | "부드럽게 / 단단하게" 가 너무 별로. 사용자가 원하는 말투를 고르게 할 것.<br>복사는 왜 있나 — 즉석에서 말하는 것으로 아는데 |
| 도움받기 | 전화 상담밖에 없음. 기관이 더 필요할 듯.<br>"내 진단 기록 보러가기" 삭제 |

**전체적으로 AI 티가 많이 남.** 화면이 예쁜 것과 별개로 문구 조정이 필요함.

---

## 시스템

회의에서 두 가지가 지적됐습니다.

- AI가 오판정하고 있음
- 하드코딩 된 부분이 있음

### 두 지적의 원인이 하나였습니다

`.env` 가 없거나 `EXPO_PUBLIC_USE_MOCK=true` 면 앱이 **목 모드**로 돌면서
`constants/mock.ts` 의 고정 결과만 돌려줍니다.

- 어떤 계약서를 넣어도 같은 판정이 나옵니다 → **"오판정" 으로 보입니다**
- 값이 고정돼 있습니다 → **"하드코딩" 으로 보입니다**

목 모드의 지문은 이렇습니다. 이 결과가 나오면 서버를 안 타고 있는 것입니다.

- 시급 = **위법소지**, 수습 감액 = **확인필요**, 나머지 6개 = 문제없음
- 분석이 정확히 **2초**에 끝남 (실제 서버는 6~9초)
- 기록함 이름이 `○○편의점`

### 9/14 에 고친 것

`lib/api.ts` 의 조건이 `USE_MOCK || !API_URL` 이어서, 서버 주소가 비어 있으면
`USE_MOCK` 이 `false` 여도 조용히 목 모드로 넘어갔습니다. 에러도 없이 정상처럼
보였습니다.

- 주소가 없으면 에러를 내도록 고쳤습니다 (PR #19)
- 주소를 코드 기본값으로 옮겨 `.env` 가 없어도 서버에 붙게 했습니다 (PR #19)
- `.env.example` 의 `USE_MOCK` 기본값을 `false` 로 바꿨습니다.
  그전에는 안내대로 복사하면 목 모드로 시작했습니다 (PR #20)

### 그래도 남은 것

**실제 오판정이 있는지는 아직 확인되지 않았습니다.**

지금까지 판정이 맞는지 확인한 것은 `scenario/` 의 PDF 를 렌더한 **깔끔한 이미지**
4장뿐입니다. 실제 종이 계약서를 폰으로 찍은 사진은 한 번도 통과하지 않았습니다.
조명, 그림자, 접힌 자국, 손글씨, 비뚤어진 각도가 전부 검증 밖입니다.

**실제 계약서를 인쇄해서 찍어보는 것이 현재 1순위입니다.**
거기서 틀리는 게 나오면 그게 진짜 오판정입니다.

---

## 추가 기능 의견

**"사람들이 잘 몰랐던 유의 사항 명시하기"**

이건 이미 기획·설계·구현이 되어 있습니다. PR #21 입니다.

야간 가산수당은 상시근로자 5인 이상 사업장에만 적용되는데 그걸 아는 사람이
드뭅니다. 사진을 보내기 전에 사업장 규모와 나이를 묻고, 결과 화면에
"몰랐을 수도 있는 것" 을 붙입니다.

- 기획: `FEATURE_hidden-conditions.md`
- 설계: `FEATURE_hidden-conditions-design.md`
- 회의에서 독립적으로 같은 요구가 나왔으므로 우선순위 근거가 됩니다

---

## 이미 반영된 것

중복 작업을 막기 위해 표시합니다.

| 회의 지적 | 상태 |
|---|---|
| 문장 화면의 복사는 왜 있나 | **제거됨** (PR #22) |
| "부드럽게 / 단단하게" 가 별로 | "정중한 말투 / 명확한 말투" 로 바뀜 (PR #22).<br>다만 **"사용자가 원하는 말투를 고르게"** 는 아직 2지선다입니다 |
| 분석중 로딩 연출 | 실측 8~9초에 맞춰 10초 기준으로 재배치. 진행 막대를 연속 애니메이션으로 (PR #24) |
| 카메라 가이드라인 | 실제 카메라 프리뷰는 `expo-camera` 미설치라 구조적 제약입니다.<br>`PROGRESS.md` 개선 방향에 이미 있습니다. 지금은 촬영 예시 삽화만 있습니다 |

---

## 연결해서 봐야 할 것

### 메인의 89% 는 R6 와 같은 건입니다

`PROGRESS.md` 의 미해결 사항 **R6** 이 이 숫자의 출처 미확보입니다.
문제 정의의 유일한 근거이고, 출처를 못 대면 발표 토대가 흔들립니다.

그래서 **카드를 늘리는 것보다 89% 를 빼는 선택이 나을 수 있습니다.**
나영웅은 같은 이유로 `analyzing.tsx` 에서 "34.8%" 를 이미 뺐고 법 조문으로
대체했습니다. 지금은 한쪽은 빼고 한쪽은 남아 있어 기준이 안 맞습니다.

### 분석중 카드 문제는 PR #18 이 고치려던 것입니다

`feat/analyzing` (PR #18)에 정보 카드를 좌우로 넘기는 작업이 있습니다.

- 실제 새 작업은 `7be4d4f` 하나입니다. 나머지 두 커밋은 이미 `develop` 에 있습니다
- 마지막 커밋 `9eca4ff` "카드 안 넘어감 재수정" 은 **빈 커밋**입니다.
  트리 해시가 부모와 같아서 바뀐 파일이 하나도 없습니다.
  고치려던 내용이 커밋에 안 들어갔습니다
- **PR #18 을 그냥 닫으면 `7be4d4f` 의 스와이프 작업도 같이 버려집니다.**
  회의에서 지적된 카드 문제가 그대로 남습니다

### 결과 화면의 "가게 종류가 이상하게 뜸"

결과 화면 상단 이름은 서버가 모델에게 읽게 한 사업장 이름(`title`)입니다.
계약서에서 제대로 못 읽으면 엉뚱한 값이 들어옵니다.

목 모드였다면 `○○편의점` 이 고정으로 떴을 것이므로, 위 시스템 항목과 같은
원인일 수 있습니다. 실제 계약서로 다시 확인할 때 같이 봐야 합니다.
